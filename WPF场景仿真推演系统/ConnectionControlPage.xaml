<Window x:Class="WPF场景仿真推演系统.ConnectionControlPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF场景仿真推演系统"
        mc:Ignorable="d"
        Title="连接统计" Height="304.167" Width="313.889">
    <Grid>
        <ListBox x:Name="LinkSurveyList" ItemsSource="{Binding}">

            <ListBox.ItemTemplate>
                <DataTemplate>

                    <ListBoxItem Content="{Binding Path=Name}" HorizontalAlignment="Left" VerticalAlignment="Center"></ListBoxItem>

                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
